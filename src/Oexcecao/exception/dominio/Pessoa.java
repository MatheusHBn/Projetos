package Oexcecao.exception.dominio;

import java.io.FileNotFoundException;

public class Pessoa{
    /*
    E outra coisa interessante é, diferente de herança, onde só pode herdar atributos de uma só classe,
    o metodo pode ter mais de uma exception (assim como as "interfaces")
     */
    public void salvar() throws LoginInvalidoException, FileNotFoundException{


    }
}
