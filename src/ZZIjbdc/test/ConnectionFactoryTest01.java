package ZZIjbdc.test;

import ZZIjbdc.dominio.Producer;
import ZZIjbdc.service.ProducerService;
import lombok.extern.log4j.Log4j2;

import java.util.List;

@Log4j2
public class ConnectionFactoryTest01 {
    /*
    Tá, aconteceu muuuita coisa antes dessas classes de Java, baixei o MySqlWorkBench, Docker e o Maven, então talvez
    os comentários ficaram meio "inúteis" por assim dizer. O que fizemos aqui foi, criar um banco de dados, e criamos
    códigos que mandem informações para ele
     */
    static void main() {
//        Producer build = Producer.builder().name("Eu memo (:").build();
//        ProducerService.save(build);
//        Producer producerToUpdate = Producer.builder().id(20).name("Google em LowerCase").build();
//        ProducerService.update(producerToUpdate);
//        List<Producer> producers = ProducerService.findByName("gle");
//        List<Producer> producers = ProducerService.findAll();
//        ProducerService.showProducerMetadata();
//        ProducerService.showDriverMetaData();
//        ProducerService.showTypeScrollWorking();
//        List<Producer> producers = ProducerService.findByNameAndUpdateToUpperCase("gle");
//        List<Producer> producers = ProducerService.findByNameInsertWhenNotFound("matheus test");
//        List<Producer> producers = ProducerService.findByNameAndDelete("matheus teste");
//        List<Producer> producers = ProducerService.findByNamePreparedStatement("math");
//        log.info(producers);
//          List<Producer> producers = ProducerService.findByNameCallableStatement("matheusz");
//          log.info(producers);
//        ProducerService.updatePreparedStatement(producerToUpdate);
    }
}
